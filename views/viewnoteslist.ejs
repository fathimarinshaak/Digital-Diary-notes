<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/bootstrap/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/style.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Gochi+Hand&family=Homemade+Apple&family=Public+Sans:ital,wght@0,100..900;1,100..900&display=swap');
    </style>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
    <title>NOTES</title>
</head>


<body class="vh-100 w-100 d-flex justify-content-center align-items-center bg-black">
    
    <div class="d-flex flex-column justify-content-between align-items-center gap-2 p-5" id="notes">

        <h1 class="h1 m-4">NOTES</h1>

        <div class="w-100 d-flex flex-column justify-content-between align-items-center overflow-auto gap-2 scroll " id="list">
          <% notes.forEach((note)=>{ %>
              <div class="note-card w-100 d-flex flex-column justify-content-start align-content-start gap-1 fw-bolder">
                  <span id="note-card__pagenumber" >Page : <%=note.pagenumber%></span>
                  <span><i class="bi bi-calendar"></i> <%=note.date%></span>
                  <p class="text-truncate"><%=note.entry%></p>
                  <a href="/viewnoteslist/<%=note.pagenumber%>" class="p-2 rounded"><i class="bi bi-journal-text"></i> View</a>                   
              </div>
          <% }) %>
        </div>

        <a href="/contents" id="back-btn" class="btn"><i class="bi bi-arrow-left"></i> back</a>

    </div>

    <script src="/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html